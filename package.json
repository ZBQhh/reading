{
  "name": "bilingual-reader",
  "private": true,
  "version": "2.7.1",
  "description": "Bilingual Digital Reader — The Atlantic & Global Journals",
  "scripts": {
    "build:js": "npx -y esbuild src/main.js --bundle --format=iife --target=es2018 --charset=utf8 --outfile=assets/js/reader_app.js",
    "build:md": "python scripts/build_markdown_articles.py",
    "build:dist": "node scripts/build-dist.js",
    "build": "npm run build:js && npm run build:md && python scripts/build_master_portal.py && node scripts/build-dist.js",
    "lint": "eslint src/",
    "test": "python scripts/stress_test_engine.py",
    "smoke": "node scripts/functional_smoke.js",
    "audit": "node scripts/browser_layout_audit.js",
    "check": "node --check assets/js/reader_app.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "eslint": "^9.39.5",
    "playwright-core": "^1.62.1"
  }
}
